import { createConstants } from '@server/utils/createConstants';

export const MODEL_NAMES = createConstants(
  'Location',
  'Category',
  'City',
  'CategoryField',
  'PhoneValidation',
  'RefreshToken',
  'User',
);
